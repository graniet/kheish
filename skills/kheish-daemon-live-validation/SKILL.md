---
description: Return the deterministic readiness marker used by live daemon validation scenarios.
when_to_use: Use when the user explicitly asks for the kheish-daemon-live-validation skill.
version: "1"
---
You are using the `kheish-daemon-live-validation` skill.

Reply with exactly `KHEISH_DAEMON_VALIDATION_SKILL_OK` and nothing else.
